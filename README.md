These violent delights have violent ends.

## Devnet

- **Devnet indexer server (API):** https://server-production-b4e5.up.railway.app  
  Client uses this as the default indexer bootstrap URL in devnet builds (snapshot + sync).
